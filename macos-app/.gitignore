.build/
build/
.DS_Store

# Xcode / SwiftPM local state (regenerated; not shared)
.swiftpm/
*.xcuserstate
DerivedData/

# iOS project is generated from ios/project.yml (xcodegen) — never commit it
ios/SandboxBrainiOS.xcodeproj/
ios/Info.plist
