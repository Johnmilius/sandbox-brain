{
  "name": "@sandbox-brain/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contract for the Brain: object types, relationships, events, wiki-links. Consumed by both the Next app and the MCP server.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
